---
# Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-demo
  namespace: nginx-demo
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-cloudflare-prod # 自动申请证书
spec:
  ingressClassName: higress
  tls:
    - hosts:
        - nginx.euxyabe.com
      secretName: nginx-euxyabe-com-tls
  rules:
    - host: nginx.euxyabe.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-demo
                port:
                  number: 80
